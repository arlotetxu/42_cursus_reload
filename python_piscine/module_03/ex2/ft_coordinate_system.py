#!/usr/bin/env python3

import sys
import math

"""
        Funcion para parseo de coordenadas?
        Funcion para calculo de distancias

        import math

class Punto3D:
    def __init__(self, x, y, z):
        self.x = x
        self.y = y
        self.z = z

    def distancia_hasta(self, otro_punto):
        # Paso 1: Diferencias
        dif_x = self.x - otro_punto.x
        dif_y = self.y - otro_punto.y
        dif_z = self.z - otro_punto.z

        # Paso 2: Cuadrados y Suma
        suma_cuadrados = (dif_x**2) + (dif_y**2) + (dif_z**2)

        # Paso 3: Raíz cuadrada
        return math.sqrt(suma_cuadrados)

# Probamos
mosca_a = Punto3D(1, 1, 1)
mosca_b = Punto3D(4, 5, 6)

# Calculamos la distancia
distancia = mosca_a.distancia_hasta(mosca_b)
print(f"La distancia es: {distancia}")
"""

my_tuple = (2, 3, 4)
print(my_tuple[0])




