{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "*.c",
                "-o",
                "${fileDirname}/${workspaceFolderBasename}"
            ],
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Compila todos los archivos .c en la carpeta"
        },
    ]
}
