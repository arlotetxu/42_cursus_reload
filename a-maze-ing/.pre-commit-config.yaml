repos:
  - repo: local
    hooks:
      - id: lint-and-build
        name: Lint (strict) and Build
        entry: bash .github/hooks/pre-commit-lint-build.sh
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
